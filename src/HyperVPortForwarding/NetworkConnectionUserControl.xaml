<UserControl x:Class="MakingWaves.Tools.HyperVPortForwarding.NetworkConnectionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{StaticResource NetworkConnectionViewModel}"
             >
    <StackPanel>
        <Button Command="{Binding AddNetworkConnectionCommand}">Add switch</Button>
        <Button Command="{Binding RemoveNetworkConnectionCommand}">Remove switch</Button>
        <Button Command="{Binding SetIpCommand}" Margin="0,50,0,0">Set ip</Button>
        <Button Command="{Binding SelectVmswitchForVmCommand}">Select VM Swtich for Virtual Machine</Button>
        <Button Command="{Binding ShareThroughEthernetCommand}">Share through Ethernet</Button>
        <Button Command="{Binding ShareThroughWifiCommand}">Share through Wifi</Button>
    </StackPanel>
</UserControl>
